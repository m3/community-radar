__pycache__/
*.pyc
.venv/
.venv311/
.env
data/
*.db
*.sqlite
*.sqlite3
.DS_Store
dist/
*.egg-info/
__pycache__/
.pytest_cache/
# Local config backup written by ConfigManager.save()
config.yaml.bak
*.tmp

# mex AST index — generated, rebuilt by `mex graph index`.
# The -wal/-shm sidecars are live SQLite journal state and change on every
# read, so tracking them produces permanent spurious diffs.
.mex/graph.db
.mex/graph.db-shm
.mex/graph.db-wal
